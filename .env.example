# ─────────────────────────────────────────────────────
# Copy this file to .env and fill in your values.
# NEVER commit .env to git — it is already in .gitignore
# ─────────────────────────────────────────────────────

# Stripe — get from dashboard.stripe.com → Developers → API Keys
# Use sk_test_... keys for testing, sk_live_... for production
# Price IDs are hardcoded in netlify/functions/create-checkout.js:
#   price_1TGlLHEPNaqEvCxlaB15AIRg = S/M/L/XL ($25)
#   price_1TGlLpEPNaqEvCxlUw5p93xU = XXL/XXXL ($30)
STRIPE_SECRET_KEY=sk_test_YOUR_STRIPE_SECRET_KEY_HERE

# Stripe Webhook Signing Secret
# Get from dashboard.stripe.com → Developers → Webhooks → your endpoint → Signing secret
# For local testing via Stripe CLI, the CLI prints a temporary secret when you run:
#   stripe listen --forward-to localhost:8888/.netlify/functions/stripe-webhook
STRIPE_WEBHOOK_SECRET=whsec_YOUR_WEBHOOK_SECRET_HERE

# Google Sheets — Sheet ID from the URL of your spreadsheet
# https://docs.google.com/spreadsheets/d/THIS_PART_HERE/edit
GOOGLE_SHEET_ID=YOUR_GOOGLE_SHEET_ID_HERE

# Google Service Account — paste the ENTIRE contents of your downloaded JSON key file
# on a single line (or as a multiline value in Netlify dashboard env vars)
GOOGLE_SERVICE_ACCOUNT_JSON={"type":"service_account","project_id":"YOUR_PROJECT_ID","private_key_id":"YOUR_KEY_ID","private_key":"-----BEGIN PRIVATE KEY-----\nYOUR_PRIVATE_KEY_HERE\n-----END PRIVATE KEY-----\n","client_email":"YOUR_SERVICE_ACCOUNT@YOUR_PROJECT.iam.gserviceaccount.com","client_id":"YOUR_CLIENT_ID","auth_uri":"https://accounts.google.com/o/oauth2/auth","token_uri":"https://oauth2.googleapis.com/token","auth_provider_x509_cert_url":"https://www.googleapis.com/oauth2/v1/certs","client_x509_cert_url":"https://www.googleapis.com/robot/v1/metadata/x509/YOUR_SERVICE_ACCOUNT%40YOUR_PROJECT.iam.gserviceaccount.com","universe_domain":"googleapis.com"}


# Set automatically by Netlify in production — used for success/cancel redirect URLs
# For local testing, netlify dev sets this to http://localhost:8888 automatically
# URL=https://your-site.netlify.app
